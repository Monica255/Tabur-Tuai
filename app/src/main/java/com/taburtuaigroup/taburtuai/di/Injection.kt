package com.taburtuaigroup.taburtuai.di


object Injection {
    /*fun provideRepository(context: Context): Repository {
        var mDb: FirebaseDatabase = FirebaseDatabase.getInstance(Repository.FIREBASE_URL)
        mDb.setPersistenceEnabled(true)
        return Repository(context,mDb)
    }*/
}